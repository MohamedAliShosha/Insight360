// This is a CategoryModel class in Dart, which has two final properties:
//  image and categoryName, both of which are required when creating an instance of the class.

class CategoryModel {
  final String image;
  final String categoryName;

  const CategoryModel({required this.image, required this.categoryName});
}
